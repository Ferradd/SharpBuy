# Cleanup 2026-08-31

Removed **~4.25 GB** of regenerable or duplicate junk. **Scripts, data/, account/, src/, api/ — not touched.**

## Deleted

| What | Size | Why safe |
|------|------|----------|
| `archive/_CAN_DELETE/` | ~4094 MB | Marked for deletion: old zips, hackershop-web copy, PyInstaller builds, SteamDesktopAuthenticator, decompiled launcher |
| `dist/` | ~140 MB | Vite build output — restore: `npm run build` |
| `build/` | ~2 MB | Launcher publish cache — restore: `pwsh scripts/publish_launcher.ps1` |
| `builds/` | ~3 MB | Duplicate `.exe` files (same as `public/SharpBuy_Launcher.exe` and `public/SharpBuy_Batch_Checker.exe`) |
| `src/launcher/SharpBuy_Launcher/bin/` | ~2 MB | .NET build artifacts |
| `src/launcher/SharpBuy_Launcher/obj/` | ~10 MB | .NET build artifacts |
| `src/launcher/SharpBuy_Launcher/publish/` | ~2 MB | Old local publish folder |
| `_login.json` | tiny | Plaintext admin password in repo root — security risk, not needed in tree |
| `archive/` (empty) | — | Removed after `_CAN_DELETE` |

## Kept (on purpose)

- **`scripts/`** — all build/test/utility scripts
- **`data/`** — tokens, orders log, steam.txt
- **`account/`** — order screenshots, credentials
- **`desktop-tools/`**, **`steam-token-tools/`**, **`lzt-analyzer/`**
- **`public/`** — site assets + launcher downloads (exe + dmg)
- **`src/`**, **`api/`**, **`node_modules/`**
- **`.env*`** — deploy config

## If you need something back

- Old site snapshot → was only in `archive/_CAN_DELETE/hackershop-web/` (git history may still have it)
- Old launcher Python GUI → `archive/_CAN_DELETE/account_login_tool_decompiled/` (deleted)
- Local `dist/` → run `npm run build`
- Local launcher exe in `builds/` → run `pwsh scripts/publish_launcher.ps1` → copies to `public/`

## `_login.json`

Contained admin email/password in plain text. If you used it as a personal reminder, re-save the password in your password manager — not in the project folder.
